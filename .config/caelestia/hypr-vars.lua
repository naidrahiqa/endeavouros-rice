return {
    terminal = "kitty",

    blurEnabled = true,
    blurSize = 12,
    blurPasses = 4,
    blurSpecialWs = true,
    blurPopups = true,
    blurInputMethods = true,

    shadowEnabled = true,
    shadowRange = 20,
    shadowRenderPower = 5,

    windowOpacity = 0.90,
    windowRounding = 15,
}
